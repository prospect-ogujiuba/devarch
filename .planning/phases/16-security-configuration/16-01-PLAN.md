---
phase: 16-security-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yml
  - .env
  - .env.example
autonomous: true

must_haves:
  truths:
    - "compose.yml references DEVARCH_API_KEY via variable interpolation, not a hardcoded value"
    - ".env contains the actual DEVARCH_API_KEY value"
    - ".env.example contains a placeholder for DEVARCH_API_KEY"
    - ".env is gitignored and never committed"
    - "API container receives DEVARCH_API_KEY from environment"
  artifacts:
    - path: "compose.yml"
      provides: "Variable-interpolated API key reference"
      contains: "${DEVARCH_API_KEY}"
    - path: ".env"
      provides: "Actual API key value"
      contains: "DEVARCH_API_KEY="
    - path: ".env.example"
      provides: "Placeholder for API key"
      contains: "DEVARCH_API_KEY="
  key_links:
    - from: "compose.yml"
      to: ".env"
      via: "Docker Compose env_file / variable interpolation"
      pattern: "\\$\\{DEVARCH_API_KEY\\}"
---

<objective>
Move hardcoded DEVARCH_API_KEY from compose.yml into .env file using Docker Compose variable interpolation.

Purpose: Prevent secret leakage in version control — API key should load from gitignored .env, not be hardcoded in committed compose.yml.
Output: compose.yml uses ${DEVARCH_API_KEY}, .env has actual value, .env.example has placeholder.
</objective>

<execution_context>
@/home/fhcadmin/.claude/get-shit-done/workflows/execute-plan.md
@/home/fhcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@compose.yml
@.env
@.env.example
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Externalize DEVARCH_API_KEY from compose.yml</name>
  <files>compose.yml, .env, .env.example</files>
  <action>
1. In compose.yml line 44, replace the hardcoded value:
   FROM: `- DEVARCH_API_KEY=682408a4c0435038e6e5085ec1f0562716ce01c7bb6cc20b53d0926430227b6d`
   TO:   `- DEVARCH_API_KEY=${DEVARCH_API_KEY}`

2. In .env, add a new section after the DOMAIN CONFIGURATION block:
   ```
   # =============================================================================
   # DEVARCH API KEY
   # =============================================================================
   DEVARCH_API_KEY=682408a4c0435038e6e5085ec1f0562716ce01c7bb6cc20b53d0926430227b6d
   ```

3. In .env.example, add the same section with a placeholder:
   ```
   # =============================================================================
   # DEVARCH API KEY
   # =============================================================================
   DEVARCH_API_KEY=your-api-key-here
   ```

4. Verify .gitignore already contains `.env` (it does at line 13 — no change needed).

NOTE: Do NOT commit .env. Only commit compose.yml and .env.example.
  </action>
  <verify>
- `grep 'DEVARCH_API_KEY=\${DEVARCH_API_KEY}' compose.yml` returns a match
- `grep 'DEVARCH_API_KEY=682408' compose.yml` returns NO match (hardcoded value removed)
- `grep 'DEVARCH_API_KEY=' .env` returns a match with the actual key
- `grep 'DEVARCH_API_KEY=' .env.example` returns a match with placeholder
- `grep '\.env' .gitignore` confirms .env is gitignored
  </verify>
  <done>
- compose.yml uses variable interpolation for DEVARCH_API_KEY
- .env contains the real API key value
- .env.example contains a safe placeholder
- .env remains gitignored
- No hardcoded API key appears in any committed file
  </done>
</task>

</tasks>

<verification>
1. `grep -n 'DEVARCH_API_KEY' compose.yml` — shows `${DEVARCH_API_KEY}`, NOT the hash
2. `grep -c 'DEVARCH_API_KEY' .env` — returns 1
3. `grep -c 'DEVARCH_API_KEY' .env.example` — returns 1
4. `git diff compose.yml` — shows hardcoded key replaced with interpolation variable
5. `docker compose config 2>/dev/null | grep DEVARCH_API_KEY` — resolves to actual key from .env (if docker available)
</verification>

<success_criteria>
- Hardcoded API key removed from compose.yml (the only committed file that had it)
- .env has the actual key, remains gitignored
- .env.example documents the required variable with placeholder
- API container still receives DEVARCH_API_KEY via environment
</success_criteria>

<output>
After completion, create `.planning/phases/16-security-configuration/16-01-SUMMARY.md`
</output>
