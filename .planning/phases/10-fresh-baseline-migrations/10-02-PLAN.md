---
phase: 10-fresh-baseline-migrations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/new_006_stacks_core.up.sql
  - api/migrations/new_006_stacks_core.down.sql
  - api/migrations/new_007_instance_overrides.up.sql
  - api/migrations/new_007_instance_overrides.down.sql
  - api/migrations/new_008_wiring_contracts_sync_security.up.sql
  - api/migrations/new_008_wiring_contracts_sync_security.down.sql
  - api/migrations/new_009_performance_indexes.up.sql
  - api/migrations/new_009_performance_indexes.down.sql
autonomous: true

must_haves:
  truths:
    - "Migrations 006-009 create stacks, instances, overrides, wiring, sync, and performance indexes"
    - "Stacks have soft delete with partial unique index on active rows"
    - "Service instances have soft delete, description, and active unique index"
    - "Instance override tables mirror service template tables for override semantics"
    - "006 adds FK constraint from projects.stack_id to stacks(id)"
    - "009 contains only non-inline indexes and autovacuum tuning, no table creation"
  artifacts:
    - path: "api/migrations/new_006_stacks_core.up.sql"
      provides: "stacks + service_instances with soft delete, plus projects.stack_id FK"
      contains: "uq_stacks_name_active"
    - path: "api/migrations/new_007_instance_overrides.up.sql"
      provides: "instance_ports, volumes, env_vars, labels, domains, healthchecks, config_files, dependencies, resource_limits"
      contains: "instance_resource_limits"
    - path: "api/migrations/new_008_wiring_contracts_sync_security.up.sql"
      provides: "service_exports, service_import_contracts, service_instance_wires, sync_state, container_states, container_metrics"
      contains: "service_instance_wires"
    - path: "api/migrations/new_009_performance_indexes.up.sql"
      provides: "BRIN index on container_metrics, images last_synced_at, vulnerabilities created_at, autovacuum tuning"
      contains: "BRIN"
  key_links:
    - from: "new_006 service_instances"
      to: "stacks(id) and services(id)"
      via: "FK references"
    - from: "new_006"
      to: "projects.stack_id"
      via: "ALTER TABLE projects ADD CONSTRAINT fk_projects_stack_id"
    - from: "new_007 all tables"
      to: "service_instances(id)"
      via: "REFERENCES service_instances(id) ON DELETE CASCADE"
    - from: "new_008 service_instance_wires"
      to: "stacks, service_instances, service_exports, service_import_contracts"
      via: "FK references to all four tables"
---

<objective>
Write fresh baseline migrations 006-009 (up + down pairs) covering stacks core, instance overrides, wiring/sync/security, and performance indexes.

Purpose: Create the stack-layer schema — stacks with soft delete, service instances, all instance override tables, wiring contracts, sync state, runtime state, and consolidated performance indexes. Completes the 9-domain migration set.

Output: 8 SQL files with `new_` prefix in api/migrations/
</objective>

<execution_context>
@/home/fhcadmin/.claude/get-shit-done/workflows/execute-plan.md
@/home/fhcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fresh-baseline-migrations/10-CONTEXT.md

Reference existing migrations for exact column types:
@api/migrations/013_stacks_instances.up.sql
@api/migrations/014_instance_overrides.up.sql
@api/migrations/015_retention_perf.up.sql
@api/migrations/016_sync_state.up.sql
@api/migrations/017_instance_dependencies.up.sql
@api/migrations/019_service_wiring.up.sql
@api/migrations/020_encryption.up.sql
@api/migrations/021_resources.up.sql
@api/migrations/023_stack_source.up.sql
@api/migrations/003_runtime_state.up.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migrations 006-007 (stacks core, instance overrides)</name>
  <files>
    api/migrations/new_006_stacks_core.up.sql
    api/migrations/new_006_stacks_core.down.sql
    api/migrations/new_007_instance_overrides.up.sql
    api/migrations/new_007_instance_overrides.down.sql
  </files>
  <action>
    Create 4 SQL files. Tables in final form.

    **new_006_stacks_core.up.sql:**
    - `stacks`: id SERIAL PK, name VARCHAR(63) NOT NULL, description TEXT DEFAULT '', network_name VARCHAR(63), source VARCHAR(32) DEFAULT 'manual', enabled BOOLEAN DEFAULT true, deleted_at TIMESTAMPTZ DEFAULT NULL, created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW()
    - Partial unique index: CREATE UNIQUE INDEX uq_stacks_name_active ON stacks(name) WHERE deleted_at IS NULL;
    - Index: idx_stacks_deleted_at ON stacks(deleted_at)
    - `service_instances`: id SERIAL PK, stack_id INT NOT NULL REFS stacks(id) ON DELETE CASCADE, instance_id VARCHAR(63) NOT NULL, template_service_id INT REFERENCES services(id), container_name VARCHAR(127), description TEXT DEFAULT '', enabled BOOLEAN DEFAULT true, deleted_at TIMESTAMPTZ DEFAULT NULL, created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW()
    - Partial unique index: CREATE UNIQUE INDEX uq_instances_stack_instance_active ON service_instances(stack_id, instance_id) WHERE deleted_at IS NULL;
    - Indexes: idx_service_instances_stack_id, idx_service_instances_template_service_id, idx_service_instances_deleted_at
    - After creating stacks, add the FK from projects: ALTER TABLE projects ADD CONSTRAINT fk_projects_stack_id FOREIGN KEY (stack_id) REFERENCES stacks(id);

    **new_006_stacks_core.down.sql:**
    - ALTER TABLE projects DROP CONSTRAINT IF EXISTS fk_projects_stack_id;
    - DROP TABLE service_instances;
    - DROP TABLE stacks;

    **new_007_instance_overrides.up.sql:**
    All tables reference service_instances(id) ON DELETE CASCADE. Create in order:
    - `instance_ports`: id SERIAL PK, instance_id INT NOT NULL REFS, host_ip VARCHAR(45) DEFAULT '0.0.0.0', host_port INT NOT NULL, container_port INT NOT NULL, protocol VARCHAR(10) DEFAULT 'tcp'
    - `instance_volumes`: id SERIAL PK, instance_id INT NOT NULL REFS, volume_type VARCHAR(20) DEFAULT 'bind', source TEXT NOT NULL, target TEXT NOT NULL, read_only BOOLEAN DEFAULT false, is_external BOOLEAN DEFAULT false
    - `instance_env_vars`: id SERIAL PK, instance_id INT NOT NULL REFS, key VARCHAR(255) NOT NULL, value TEXT, is_secret BOOLEAN DEFAULT false, encrypted_value TEXT, encryption_version INT NOT NULL DEFAULT 0, UNIQUE(instance_id, key)
    - `instance_labels`: id SERIAL PK, instance_id INT NOT NULL REFS, key VARCHAR(255) NOT NULL, value TEXT NOT NULL, UNIQUE(instance_id, key)
    - `instance_domains`: id SERIAL PK, instance_id INT NOT NULL REFS, domain VARCHAR(255) NOT NULL, proxy_port INT
    - `instance_healthchecks`: id SERIAL PK, instance_id INT NOT NULL REFS ON DELETE CASCADE UNIQUE, test TEXT NOT NULL, interval_seconds INT DEFAULT 30, timeout_seconds INT DEFAULT 10, retries INT DEFAULT 3, start_period_seconds INT DEFAULT 0
    - `instance_config_files`: id SERIAL PK, instance_id INT NOT NULL REFS, file_path TEXT NOT NULL, content TEXT NOT NULL, file_mode VARCHAR(10) DEFAULT '0644', is_template BOOLEAN DEFAULT false, created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW(), UNIQUE(instance_id, file_path)
    - `instance_dependencies`: id SERIAL PK, instance_id INT NOT NULL REFS, depends_on TEXT NOT NULL, condition VARCHAR(32) DEFAULT 'service_started', UNIQUE(instance_id, depends_on)
    - `instance_resource_limits`: id SERIAL PK, instance_id INT NOT NULL REFS, cpu_limit TEXT, cpu_reservation TEXT, memory_limit TEXT, memory_reservation TEXT, created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW(), UNIQUE(instance_id)
    - Indexes on instance_id for each table: idx_instance_ports_instance_id, idx_instance_volumes_instance_id, idx_instance_env_vars_instance_id, idx_instance_labels_instance_id, idx_instance_domains_instance_id, idx_instance_config_files_instance_id, idx_instance_dependencies_instance_id

    **new_007_instance_overrides.down.sql:**
    - DROP in reverse: instance_resource_limits, instance_dependencies, instance_config_files, instance_healthchecks, instance_domains, instance_labels, instance_env_vars, instance_volumes, instance_ports
  </action>
  <verify>
    All 4 files exist. Grep for "uq_stacks_name_active" in new_006 — should match. Grep for "fk_projects_stack_id" in new_006 up AND down — should match both. Grep for "instance_resource_limits" in new_007 — should match. Grep for "CASCADE" in down files — should find ZERO matches. Count tables in new_007 up file — should be 9 CREATE TABLE statements.
  </verify>
  <done>
    4 SQL files written. Stacks have soft delete + partial unique index + source column. Service instances have description + soft delete. 006 adds FK from projects.stack_id. All 9 instance override tables created in 007 including resource_limits. Down files use precise drops.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migrations 008-009 (wiring/sync/security, performance indexes)</name>
  <files>
    api/migrations/new_008_wiring_contracts_sync_security.up.sql
    api/migrations/new_008_wiring_contracts_sync_security.down.sql
    api/migrations/new_009_performance_indexes.up.sql
    api/migrations/new_009_performance_indexes.down.sql
  </files>
  <action>
    Create 4 SQL files.

    **new_008_wiring_contracts_sync_security.up.sql:**
    Wiring tables:
    - `service_exports`: id SERIAL PK, service_id INT NOT NULL REFS services(id) ON DELETE CASCADE, name VARCHAR(128) NOT NULL, type VARCHAR(64) NOT NULL, port INT NOT NULL, protocol VARCHAR(10) DEFAULT 'tcp', UNIQUE(service_id, name)
    - `service_import_contracts`: id SERIAL PK, service_id INT NOT NULL REFS services(id) ON DELETE CASCADE, name VARCHAR(128) NOT NULL, type VARCHAR(64) NOT NULL, required BOOLEAN DEFAULT true, env_vars JSONB DEFAULT '{}', UNIQUE(service_id, name)
    - `service_instance_wires`: id SERIAL PK, stack_id INT NOT NULL REFS stacks(id) ON DELETE CASCADE, consumer_instance_id INT NOT NULL REFS service_instances(id) ON DELETE CASCADE, provider_instance_id INT NOT NULL REFS service_instances(id) ON DELETE CASCADE, import_contract_id INT NOT NULL REFS service_import_contracts(id), export_contract_id INT NOT NULL REFS service_exports(id), source VARCHAR(10) NOT NULL DEFAULT 'auto' CHECK (source IN ('auto','explicit')), provider_contract_type VARCHAR(64), consumer_contract_type VARCHAR(64), created_at TIMESTAMPTZ DEFAULT NOW(), UNIQUE(stack_id, consumer_instance_id, import_contract_id)

    Sync table:
    - `sync_state`: key TEXT PRIMARY KEY, value TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    - NO seed INSERT — no initial sync_state row (importer is sole data path per decision; sync_state init is runtime concern)

    Runtime state tables:
    - `container_states`: id SERIAL PK, service_id INT REFS services(id) ON DELETE CASCADE UNIQUE, container_id VARCHAR(64), status VARCHAR(32) NOT NULL, health_status VARCHAR(32), restart_count INT DEFAULT 0, started_at TIMESTAMPTZ, finished_at TIMESTAMPTZ, exit_code INT, error TEXT, updated_at TIMESTAMPTZ DEFAULT NOW()
    - `container_metrics`: id SERIAL PK, service_id INT REFS services(id) ON DELETE CASCADE, cpu_percentage DECIMAL(5,2), memory_used_mb DECIMAL(10,2), memory_limit_mb DECIMAL(10,2), memory_percentage DECIMAL(5,2), network_rx_bytes BIGINT, network_tx_bytes BIGINT, recorded_at TIMESTAMPTZ DEFAULT NOW()

    Indexes:
    - idx_service_exports_service_id, idx_service_import_contracts_service_id
    - idx_service_instance_wires_stack_id, idx_service_instance_wires_consumer, idx_service_instance_wires_provider
    - idx_container_states_service, idx_container_states_status
    - idx_container_metrics_service_time ON container_metrics(service_id, recorded_at DESC)

    **new_008_wiring_contracts_sync_security.down.sql:**
    - DROP: container_metrics, container_states, sync_state, service_instance_wires, service_import_contracts, service_exports (reverse dependency order)

    **new_009_performance_indexes.up.sql:**
    Non-inline performance indexes and autovacuum tuning. Only indexes NOT already created inline in 001-008:
    - CREATE INDEX idx_container_metrics_recorded_at_brin ON container_metrics USING BRIN (recorded_at);
    - CREATE INDEX idx_images_last_synced_at ON images(last_synced_at);
    - CREATE INDEX idx_vulnerabilities_created_at ON vulnerabilities(created_at);
    - ALTER TABLE container_metrics SET (autovacuum_vacuum_scale_factor = 0.02, autovacuum_analyze_scale_factor = 0.01, autovacuum_vacuum_threshold = 5000, autovacuum_analyze_threshold = 5000);

    **new_009_performance_indexes.down.sql:**
    - DROP INDEX idx_container_metrics_recorded_at_brin;
    - DROP INDEX idx_images_last_synced_at;
    - DROP INDEX idx_vulnerabilities_created_at;
    - Reset autovacuum: ALTER TABLE container_metrics RESET (autovacuum_vacuum_scale_factor, autovacuum_analyze_scale_factor, autovacuum_vacuum_threshold, autovacuum_analyze_threshold);
    - Per decision: 009 down drops ONLY non-inline performance indexes; inline constraints die with their tables.
  </action>
  <verify>
    All 4 files exist. Grep for "service_instance_wires" in new_008 — should match. Grep for "INSERT" in new_008 — should find ZERO matches (no sync_state seed). Grep for "BRIN" in new_009 — should match. Grep for "CREATE TABLE" in new_009 — should find ZERO matches (indexes only). new_009 down should contain DROP INDEX and ALTER TABLE RESET, nothing else.
  </verify>
  <done>
    4 SQL files written. Wiring tables created with all FK references. sync_state has no seed data. container_states and container_metrics in 008. 009 has only non-inline indexes and autovacuum tuning. Down files are precise reverse operations.
  </done>
</task>

</tasks>

<verification>
- All 8 SQL files exist in api/migrations/ with `new_` prefix
- No ALTER TABLE in up files (except 006 adding FK to projects)
- No INSERT/seed data in any file
- No CASCADE in any down file
- Stacks have soft delete + source column
- Service instances have description + soft delete
- 006 creates FK constraint for projects.stack_id
- All 9 instance override tables in 007
- Wiring tables in 008 with correct multi-table FK references
- No sync_state seed in 008
- 009 has ONLY indexes and autovacuum, no tables
</verification>

<success_criteria>
8 SQL files written covering stacks (soft delete, source), service instances (description, soft delete), all instance override tables including resource_limits, wiring/contracts/sync/runtime tables, and consolidated performance indexes. Down files precise and CASCADE-free. 006 wires projects.stack_id FK to stacks.
</success_criteria>

<output>
After completion, create `.planning/phases/10-fresh-baseline-migrations/10-02-SUMMARY.md`
</output>
