---
phase: 25-websocket-expansion-frontend-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/hooks/use-websocket.ts
autonomous: true

must_haves:
  truths:
    - "WS status messages invalidate all stack queries (list, detail, network, compose, wires)"
    - "WS status messages invalidate all instance queries (list, detail, effective-config, resources)"
    - "WS auth token included in connection URL when API auth enabled (Phase 18 delivery)"
    - "Existing service/status/category invalidation unchanged"
  artifacts:
    - path: "dashboard/src/hooks/use-websocket.ts"
      provides: "Stack and instance query invalidation on WS status messages"
      contains: "key[0] === 'stacks'"
  key_links:
    - from: "dashboard/src/hooks/use-websocket.ts"
      to: "dashboard/src/features/stacks/queries.ts"
      via: "predicate matching queryKey[0] === 'stacks'"
      pattern: "key\\[0\\] === 'stacks'"
    - from: "dashboard/src/hooks/use-websocket.ts"
      to: "dashboard/src/features/instances/queries.ts"
      via: "same predicate (instances use ['stacks', stackName, 'instances', ...] keys)"
      pattern: "key\\[0\\] === 'stacks'"
---

<objective>
Add stack and instance query invalidation to WebSocket status handler so live container status updates refresh stack/instance detail pages.

Purpose: Close FE-05 — stack and instance queries currently only refresh via 30s polling; WS broadcasts should trigger immediate refetch.
Output: Updated use-websocket.ts with stacks predicate invalidation.
</objective>

<execution_context>
@/home/fhcadmin/.claude/get-shit-done/workflows/execute-plan.md
@/home/fhcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@dashboard/src/hooks/use-websocket.ts
@dashboard/src/features/stacks/queries.ts
@dashboard/src/features/instances/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stacks predicate invalidation to WS status handler</name>
  <files>dashboard/src/hooks/use-websocket.ts</files>
  <action>
In the `ws.onmessage` handler inside `useWebSocket`, after the existing `queryClient.invalidateQueries` calls (lines 53-59), add a single predicate-based invalidation that matches all query keys starting with `'stacks'`:

```typescript
queryClient.invalidateQueries({ predicate: (q) => {
  const key = q.queryKey
  return Array.isArray(key) && key[0] === 'stacks'
}})
```

This single predicate covers ALL stack and instance queries because:
- Stack queries: `['stacks']`, `['stacks', name]`, `['stacks', name, 'network']`, `['stacks', name, 'compose']`, `['stacks', name, 'wires']`, `['stacks', 'trash']`
- Instance queries: `['stacks', stackName, 'instances']`, `['stacks', stackName, 'instances', id]`, `['stacks', stackName, 'instances', id, 'effective-config']`, `['stacks', stackName, 'instances', id, 'resources']`

All under `['stacks', ...]` prefix so one predicate covers SC-1 (stack detail), SC-2 (instance detail), and SC-4 (live refresh).

Note: The existing `['services']`, `['status']`, `['categories']`, and service metrics predicate invalidation lines MUST remain unchanged. The stacks predicate is purely additive.

Also note: The existing `queryClient.invalidateQueries({ queryKey: ['services'] })` call already covers the `['services']` key. Since we are now adding a broad `['stacks', ...]` predicate, consider whether the existing specific `['services']` and `['categories']` calls could be consolidated into predicates for consistency. Per user discretion, this is optional — keeping them as-is is fine since they work correctly.

WS auth (SC-3) is already delivered by Phase 18 — `fetchWSToken()` on line 33 fetches a signed token, and the `?token=` query param on line 38 sends it. No changes needed for auth.
  </action>
  <verify>
1. `cd /home/fhcadmin/projects/devarch/dashboard && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd /home/fhcadmin/projects/devarch/dashboard && npx eslint src/hooks/use-websocket.ts` — No lint errors
3. Grep confirms predicate exists: `grep "stacks" dashboard/src/hooks/use-websocket.ts` shows the new predicate
4. Grep confirms existing invalidations preserved: `grep "services\|status\|categories\|metrics" dashboard/src/hooks/use-websocket.ts` shows all 4 existing patterns
  </verify>
  <done>
WS status messages invalidate all `['stacks', ...]` queries via predicate. Existing service/status/category/metrics invalidation unchanged. File compiles and lints clean.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes
2. ESLint passes on modified file
3. Existing invalidation lines (services, status, categories, metrics predicate) still present
4. New stacks predicate present and covers `key[0] === 'stacks'`
5. WS auth token flow intact (fetchWSToken + ?token= query param unchanged)
</verification>

<success_criteria>
- `use-websocket.ts` invalidates all `['stacks', ...]` query keys on status message
- Existing invalidation behavior unchanged
- WS auth (Phase 18) verified intact — fetchWSToken + token query param present
- File compiles and lints clean
</success_criteria>

<output>
After completion, create `.planning/phases/25-websocket-expansion-frontend-auth/25-01-SUMMARY.md`
</output>
