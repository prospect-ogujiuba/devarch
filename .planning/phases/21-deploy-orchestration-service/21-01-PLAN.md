---
phase: 21-deploy-orchestration-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/internal/orchestration/service.go
  - api/internal/orchestration/errors.go
autonomous: true

must_haves:
  truths:
    - "GeneratePlan method loads stack, instances, running containers, computes diff, resolves wiring, loads resources, and returns plan.Plan"
    - "ApplyPlan method validates token, acquires advisory lock, creates network, generates compose YAML, runs compose up"
    - "ResolveWiring method loads providers/consumers/existing wires, resolves auto-wires, validates, persists in transaction"
    - "Service methods accept Go types only — no net/http imports"
    - "Sentinel errors enable handlers to map service errors to HTTP status codes"
  artifacts:
    - path: "api/internal/orchestration/service.go"
      provides: "Orchestration service with GeneratePlan, ApplyPlan, ResolveWiring"
      contains: "func (s *Service) GeneratePlan"
    - path: "api/internal/orchestration/errors.go"
      provides: "Sentinel error definitions"
      contains: "ErrStackNotFound"
  key_links:
    - from: "api/internal/orchestration/service.go"
      to: "api/internal/plan"
      via: "import and usage of plan.ComputeDiff, plan.GenerateToken, plan.ValidateToken"
      pattern: "plan\\.ComputeDiff|plan\\.GenerateToken|plan\\.ValidateToken"
    - from: "api/internal/orchestration/service.go"
      to: "api/internal/wiring"
      via: "import and usage of wiring.ResolveAutoWires, wiring.ValidateWiring"
      pattern: "wiring\\.ResolveAutoWires|wiring\\.ValidateWiring"
    - from: "api/internal/orchestration/service.go"
      to: "api/internal/compose"
      via: "import and usage of compose.NewGenerator"
      pattern: "compose\\.NewGenerator"
---

<objective>
Create the orchestration service package that encapsulates plan generation, plan application, and wiring resolution business logic.

Purpose: Extract deploy orchestration logic from HTTP handlers into a transport-agnostic service layer, enabling testability and reuse.
Output: `api/internal/orchestration/service.go` with three public methods (GeneratePlan, ApplyPlan, ResolveWiring) and `errors.go` with sentinel errors.
</objective>

<execution_context>
@/home/fhcadmin/.claude/get-shit-done/workflows/execute-plan.md
@/home/fhcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deploy-orchestration-service/21-RESEARCH.md

@api/internal/api/handlers/stack_plan.go
@api/internal/api/handlers/stack_apply.go
@api/internal/api/handlers/stack_wiring.go
@api/internal/api/handlers/stack.go
@api/internal/plan/types.go
@api/internal/plan/staleness.go
@api/internal/wiring/resolver.go
@api/internal/wiring/env_injector.go
@api/internal/wiring/validator.go
@api/internal/container/client.go
@api/internal/compose/generator.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create orchestration service with GeneratePlan, ApplyPlan, ResolveWiring</name>
  <files>api/internal/orchestration/service.go, api/internal/orchestration/errors.go</files>
  <action>
Create `api/internal/orchestration/` package with two files:

**errors.go** — Sentinel errors for handler mapping:
```go
var (
    ErrStackNotFound  = errors.New("stack not found")
    ErrStackDisabled  = errors.New("stack is disabled")
    ErrStalePlan      = errors.New("plan is stale")
    ErrLockConflict   = errors.New("stack is being applied by another session")
    ErrProjectRoot    = errors.New("PROJECT_ROOT not set")
    ErrValidation     = errors.New("validation failed") // wrappable with fmt.Errorf
)
```

**service.go** — Service struct + three public methods + private helpers:

Service struct with dependencies:
```go
type Service struct {
    db              *sql.DB
    containerClient *container.Client
}
func NewService(db *sql.DB, cc *container.Client) *Service
```

**GeneratePlan(stackName string) (*plan.Plan, error)** — Extract from `stack_plan.go:Plan()` lines 30-149:
1. Query stack row (id, network_name, updated_at, enabled) — return ErrStackNotFound or ErrStackDisabled
2. Query service instances for stack (instance_id, template_name, container_name, enabled, updated_at)
3. Build desired instances slice and timestamps using container.ContainerName for name computation
4. Call containerClient.ListContainersWithLabels for running containers (graceful: empty on error)
5. Call plan.ComputeDiff, plan.GenerateToken
6. Call resolveAndBuildWiring (private method — extract from stack_plan.go lines 195-272)
7. Call loadResourceLimitsForStack (private method — extract from stack_plan.go lines 152-193)
8. Assemble and return plan.Plan

**ApplyPlan(ctx context.Context, stackName string, token string, lockFile *lock.LockFile) (*ApplyResult, error)** — Extract from `stack_apply.go:Apply()` lines 55-182:
- NOTE: ctx needed for advisory lock acquire/release
- Define ApplyResult struct: `type ApplyResult struct { Output string; LockWarnings []string }`
1. Query stack (id, network_name, enabled) — return ErrStackNotFound or ErrStackDisabled
2. Acquire advisory lock via pg_try_advisory_lock — return ErrLockConflict if not acquired
3. Defer advisory unlock with 5s timeout context
4. Call plan.ValidateToken — wrap plan.ErrStalePlan as orchestration.ErrStalePlan
5. Resolve network name (custom or devarch-{name}-net)
6. Create network via containerClient.CreateNetwork
7. Read PROJECT_ROOT, HOST_PROJECT_ROOT, WORKSPACE_ROOT env vars
8. Create compose generator, set roots, materialize configs, generate YAML
9. Write YAML to temp file, run compose via containerClient.RunCompose
10. If lockFile provided, run lock.Validator and collect warnings
11. Return ApplyResult with output and optional lock warnings
- Clean up config dir on compose generation errors (same pattern as current handler)

**ResolveWiring(stackName string) (*ResolveResult, error)** — Extract from `stack_wiring.go:ResolveWires()` lines 200-329:
- Define ResolveResult struct: `type ResolveResult struct { ResolvedCount int; Warnings []string }`
1. Query stack ID — return ErrStackNotFound
2. Load providers using loadProviders (private, extract from stack_plan.go:274-296)
3. Load consumers using loadConsumers (private, extract from stack_plan.go:298-327)
4. Load existing wires using loadExistingWires (private, extract from stack_plan.go:329-349)
5. Call wiring.ResolveAutoWires
6. Call wiring.ValidateWiring — if validation error, return wrapped ErrValidation
7. Transaction: delete auto wires, insert candidates, commit
8. Return ResolveResult

**Private helpers** (extract from stack_plan.go, keep lowercase):
- `loadProviders(stackID int) ([]wiring.Provider, error)` — from stack_plan.go lines 274-296
- `loadConsumers(stackID int) ([]wiring.Consumer, error)` — from stack_plan.go lines 298-327
- `loadExistingWires(stackID int) ([]wiring.ExistingWire, error)` — from stack_plan.go lines 329-349
- `loadAllWires(stackID int, stackName string) ([]plan.WirePlanEntry, error)` — from stack_plan.go lines 351-415
- `resolveAndBuildWiring(stackID int, stackName string) (*plan.WiringSection, []string, error)` — from stack_plan.go lines 195-272
- `loadResourceLimitsForStack(stackID int) (map[string]plan.ResourceLimitEntry, []string, error)` — from stack_plan.go lines 152-193

CRITICAL: Service must NOT import net/http. Service accepts/returns Go types only. Error signaling via sentinel errors and fmt.Errorf wrapping. The handler will map errors to HTTP status codes in Plan 02.

NOTE: The ResolveWires handler (stack_wiring.go) has DIFFERENT provider/consumer loading logic than the plan handler's loadProviders/loadConsumers — specifically the ResolveWires query also loads providers from disabled instances (`si.enabled = true` is NOT in the plan handler's loadProviders but IS in the wiring handler's version). Examine both carefully and note that:
- stack_plan.go:loadProviders has `AND si.enabled = true` — only enabled providers
- stack_wiring.go:ResolveWires queries providers without the enabled filter in its inline query (it uses `si.deleted_at IS NULL` only)
- Wait — actually looking more carefully, stack_plan.go:loadProviders uses `si.enabled = true`, while stack_wiring.go:ResolveWires inline query does NOT have this filter.
- The service's ResolveWiring method should use its OWN provider loading (matching the current ResolveWires handler behavior, which does NOT filter by enabled). Use the private loadProviders helper for GeneratePlan only, and inline/add separate query for ResolveWiring.
- ACTUALLY, re-reading stack_wiring.go more carefully: the provider query at line 216-222 does NOT have `si.enabled = true`. But the private loadProviders at stack_plan.go:274 DOES have it. So they differ. For ResolveWiring, use query WITHOUT enabled filter to match current behavior. Add a `loadAllProviders(stackID int)` helper for this.
  </action>
  <verify>
    1. `cd /home/fhcadmin/projects/devarch/api && go build ./internal/orchestration/` compiles successfully
    2. Service struct has db and containerClient fields
    3. Three public methods exist: GeneratePlan, ApplyPlan, ResolveWiring
    4. `grep -r "net/http" api/internal/orchestration/` returns nothing
    5. errors.go defines ErrStackNotFound, ErrStackDisabled, ErrStalePlan, ErrLockConflict
  </verify>
  <done>
    - orchestration.Service compiles with zero errors
    - GeneratePlan returns *plan.Plan with changes, token, wiring, resource limits
    - ApplyPlan accepts context, returns ApplyResult with output
    - ResolveWiring returns ResolveResult with resolved count and warnings
    - No net/http import in orchestration package
    - Sentinel errors defined for all error cases handlers need to distinguish
  </done>
</task>

</tasks>

<verification>
1. `cd api && go build ./internal/orchestration/` — package compiles
2. `grep -c "func (s \*Service)" api/internal/orchestration/service.go` — returns 3+ (public methods)
3. `grep -r "net/http" api/internal/orchestration/` — returns nothing (no HTTP coupling)
4. All sentinel errors defined in errors.go
</verification>

<success_criteria>
- Orchestration package compiles independently
- Three public methods (GeneratePlan, ApplyPlan, ResolveWiring) contain all business logic currently in handlers
- Service is transport-agnostic (no HTTP imports)
- Sentinel errors enable clean error-to-status-code mapping in handlers
</success_criteria>

<output>
After completion, create `.planning/phases/21-deploy-orchestration-service/21-01-SUMMARY.md`
</output>
