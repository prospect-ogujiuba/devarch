---
phase: 13-import-scalability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/internal/api/handlers/stack_import.go
  - api/internal/api/routes.go
autonomous: true

must_haves:
  truths:
    - "Stack import reads multipart body part-by-part without buffering entire form"
    - "Stack import endpoint accepts payloads up to 256MB (configurable via STACK_IMPORT_MAX_BYTES)"
    - "All non-import endpoints reject payloads over 10MB"
  artifacts:
    - path: "api/internal/api/handlers/stack_import.go"
      provides: "Streaming multipart import handler"
      contains: "multipart.NewReader"
    - path: "api/internal/api/routes.go"
      provides: "Route-level size override for import"
      contains: "STACK_IMPORT_MAX_BYTES"
  key_links:
    - from: "api/internal/api/routes.go"
      to: "api/internal/api/middleware/middleware.go"
      via: "r.With(mw.MaxBodySize(importMax))"
      pattern: "r\\.With\\(mw\\.MaxBodySize"
    - from: "api/internal/api/handlers/stack_import.go"
      to: "mime/multipart"
      via: "multipart.NewReader replaces ParseMultipartForm"
      pattern: "multipart\\.NewReader"
---

<objective>
Replace full-buffering multipart parse with streaming reader and add route-specific size cap for stack import.

Purpose: Prevent OOM on large stack imports; decouple import size limit from global 10MB cap.
Output: Handler uses `multipart.NewReader` + `io.LimitReader`; import route has dedicated 256MB limit via env var.
</objective>

<execution_context>
@/home/fhcadmin/.claude/get-shit-done/workflows/execute-plan.md
@/home/fhcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/internal/api/handlers/stack_import.go
@api/internal/api/routes.go
@api/internal/api/middleware/middleware.go
@api/internal/export/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ParseMultipartForm with streaming multipart reader</name>
  <files>api/internal/api/handlers/stack_import.go</files>
  <action>
Rewrite ImportStack handler to use streaming multipart read:

1. Extract boundary from Content-Type header via `mime.ParseMediaType(r.Header.Get("Content-Type"))` — get `params["boundary"]`.
2. Create `multipart.NewReader(r.Body, boundary)` — reads part-by-part, never buffers full form.
3. Loop `reader.NextPart()` until finding part with form name "file".
4. Read `STACK_IMPORT_MAX_BYTES` env var (default `256 << 20`). Apply `io.LimitReader(part, importMax)` on the part — caps YAML content size, not multipart overhead. Per user decision: limit targets part, not body.
5. Feed limited part directly to `yaml.NewDecoder(limitedPart).Decode(&devarchFile)` — no intermediate buffer.
6. Remove the `r.ParseMultipartForm(10 << 20)` and `r.FormFile("file")` calls entirely.
7. Keep all validation logic after decode (version check, name check, ValidateName) unchanged.
8. On `io.LimitReader` exhaustion, yaml.Decoder returns an error — handle with appropriate "payload too large" HTTP 413 response.

Import paths needed: `"io"`, `"mime"`, `"mime/multipart"`. Remove `"fmt"` only if no longer used (it is — keep it).
  </action>
  <verify>
`cd /home/fhcadmin/projects/devarch/api && go build ./...` compiles without errors.
Grep for `ParseMultipartForm` in stack_import.go returns zero results.
Grep for `multipart.NewReader` in stack_import.go returns one result.
  </verify>
  <done>
Handler reads multipart body via streaming reader. No ParseMultipartForm call. io.LimitReader caps YAML part at configurable limit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add route-level size override for import endpoint</name>
  <files>api/internal/api/routes.go</files>
  <action>
Add route-specific body size override for the stack import endpoint:

1. At the top of `NewRouter`, read `STACK_IMPORT_MAX_BYTES` env var. Parse as int64. Default to `256 << 20` (268435456). Use `os.Getenv` + `strconv.ParseInt`.
2. Change the import route registration from:
   `r.Post("/import", stackHandler.ImportStack)`
   to:
   `r.With(mw.MaxBodySize(importMax)).Post("/import", stackHandler.ImportStack)`
   This applies 256MB limit only to the import route. Per user decision: chi processes innermost MaxBytesReader, so route-level replaces global 10MB.
3. Global `r.Use(mw.MaxBodySize(10 << 20))` on line 30 stays untouched — all non-import routes keep 10MB default.
4. Add `"os"` and `"strconv"` to imports if not already present.
  </action>
  <verify>
`cd /home/fhcadmin/projects/devarch/api && go build ./...` compiles without errors.
Grep for `STACK_IMPORT_MAX_BYTES` in routes.go returns result.
Grep for `r.With(mw.MaxBodySize` in routes.go returns result for import route.
Verify `r.Use(mw.MaxBodySize(10 << 20))` still present (global limit unchanged).
  </verify>
  <done>
Import endpoint has dedicated 256MB size cap via env var. Global 10MB cap unchanged for all other endpoints. Route uses chi `r.With()` pattern for additive override.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/fhcadmin/projects/devarch/api && go build ./...` — all packages compile
2. No `ParseMultipartForm` in codebase (replaced by streaming)
3. `STACK_IMPORT_MAX_BYTES` env var read in routes.go
4. `multipart.NewReader` used in handler
5. `io.LimitReader` applied to multipart part (not body)
6. Global 10MB body limit still applied via `r.Use()`
</verification>

<success_criteria>
- Stack import handler uses streaming multipart read (no ParseMultipartForm)
- Route-specific 256MB cap via STACK_IMPORT_MAX_BYTES env var
- Global 10MB cap preserved for all other endpoints
- Code compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/13-import-scalability/13-01-SUMMARY.md`
</output>
