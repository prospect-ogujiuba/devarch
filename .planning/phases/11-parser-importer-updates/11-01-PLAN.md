---
phase: 11-parser-importer-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/internal/compose/parser.go
  - api/internal/compose/importer.go
autonomous: true

must_haves:
  truths:
    - "Parser extracts env_file (string and list forms) into ParsedService.EnvFiles []string"
    - "Parser extracts container_name into ParsedService.ContainerName string"
    - "Parser extracts network names into ParsedService.Networks []string"
    - "Importer writes env_files to service_env_files table with sort_order preserving declaration order"
    - "Importer writes container_name to services.container_name_template column"
    - "Importer writes network names to service_networks table"
  artifacts:
    - path: "api/internal/compose/parser.go"
      provides: "EnvFiles, ContainerName, Networks fields on ParsedService"
      contains: "EnvFiles"
    - path: "api/internal/compose/importer.go"
      provides: "DB writes for env_files, container_name, networks"
      contains: "service_env_files"
  key_links:
    - from: "api/internal/compose/parser.go"
      to: "api/internal/compose/importer.go"
      via: "ParsedService struct fields consumed by importService"
      pattern: "parsed\\.EnvFiles|parsed\\.ContainerName|parsed\\.Networks"
    - from: "api/internal/compose/importer.go"
      to: "service_env_files table"
      via: "INSERT INTO service_env_files"
      pattern: "INSERT INTO service_env_files"
    - from: "api/internal/compose/importer.go"
      to: "service_networks table"
      via: "INSERT INTO service_networks"
      pattern: "INSERT INTO service_networks"
---

<objective>
Add env_file, container_name, and network parsing to the compose parser, and write these fields to the new schema tables during import.

Purpose: PARS-01, PARS-02, PARS-03 — preserve env_file paths, container_name template, and network attachments that were previously parsed-but-discarded or not stored with proper schema support.
Output: Updated parser.go with new ParsedService fields, updated importer.go with DB writes to service_env_files, services.container_name_template, and service_networks.
</objective>

<execution_context>
@/home/fhcadmin/.claude/get-shit-done/workflows/execute-plan.md
@/home/fhcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-parser-importer-updates/11-CONTEXT.md
@api/internal/compose/parser.go
@api/internal/compose/importer.go
@api/migrations/001_categories_services.up.sql
@api/migrations/002_service_runtime_shape.up.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add env_file, container_name, and networks parsing to ParsedService</name>
  <files>api/internal/compose/parser.go</files>
  <action>
Add three new fields to ParsedService struct:
- `EnvFiles []string` — env_file paths in declaration order
- `ContainerName string` — container_name value
- `Networks []string` — network names service attaches to

In ParseFileAll, after existing parsing (line ~157), add:
1. `parsed.ContainerName = svc.ContainerName` — direct field copy from ComposeService (already has `yaml:"container_name"`)
2. `parsed.EnvFiles = parseEnvFile(svc.EnvFile)` — new function
3. `parsed.Networks = parseNetworks(svc.Networks)` — new function

New function `parseEnvFile(envFile interface{}) []string`:
- nil → return nil
- string → return []string{s} (single string form)
- []interface{} → convert each element to string, return slice
- Preserves original relative paths as-is (e.g., "../../.env")
- Per decision: store regardless of whether file exists on disk

New function `parseNetworks(networks interface{}) []string`:
- nil → return nil
- ComposeService.Networks is `interface{}` — can be []interface{} (simple list) or map[string]interface{} (network config)
- []interface{} → extract string names
- map[string]interface{} → extract map keys (network names only, ignore config per PARS-03 decision)
- Note: This is NOT the top-level networks block — this is the per-service `networks:` list

Both functions follow the exact pattern of parseEnvironment/parseDependsOn (interface{} type switch).
  </action>
  <verify>
`cd /home/fhcadmin/projects/devarch/api && go build ./...` — compiles without error.
Grep for EnvFiles, ContainerName, Networks fields in parser.go to confirm they exist.
  </verify>
  <done>
ParsedService has EnvFiles, ContainerName, Networks fields populated from compose YAML. String env_file normalized to 1-element slice. Network names extracted from both list and map forms.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write env_files, container_name, and networks to DB in importer</name>
  <files>api/internal/compose/importer.go</files>
  <action>
In `importService()`, add DB writes for the three new fields:

1. **container_name_template** — update the services INSERT/UPSERT to include container_name_template column:
   - Add `container_name_template` to INSERT columns and ON CONFLICT SET
   - Use `NULLIF(parsed.ContainerName, '')` to store NULL when empty
   - This goes into the existing services INSERT query (line ~284), NOT a separate table

2. **service_env_files** — after the service_labels block (line ~356), add DELETE + INSERT pattern:
   ```
   DELETE FROM service_env_files WHERE service_id = $1
   ```
   Then loop with sort_order:
   ```
   INSERT INTO service_env_files (service_id, path, sort_order)
   VALUES ($1, $2, $3)
   ```
   - sort_order = loop index (0, 1, 2...) preserving declaration order per decision
   - Table has UNIQUE(service_id, path) — DELETE first avoids conflicts

3. **service_networks** — after env_files block, add DELETE + INSERT pattern:
   ```
   DELETE FROM service_networks WHERE service_id = $1
   ```
   Then loop:
   ```
   INSERT INTO service_networks (service_id, network_name)
   VALUES ($1, $2)
   ```
   - Table has UNIQUE(service_id, network_name) — DELETE first avoids conflicts

All three writes happen inside the existing tx transaction in importService(), before tx.Commit().
Follow the exact DELETE + INSERT pattern used for ports, volumes, env_vars (lines 303-356).
  </action>
  <verify>
`cd /home/fhcadmin/projects/devarch/api && go build ./...` — compiles without error.
Run import against test compose files if DB available:
```
cd /home/fhcadmin/projects/devarch && docker compose up -d postgres && sleep 2 && cd api && go run ./cmd/migrate -migrations ./migrations && go run ./cmd/import -compose-dir ../old-compose-and-configs/compose -config-dir ../old-compose-and-configs/config
```
Then verify data:
```sql
SELECT COUNT(*) FROM service_env_files;       -- should be ~173 (all services have env_file)
SELECT COUNT(*) FROM service_networks;         -- should be ~173 (all attach to microservices-net)
SELECT COUNT(*) FROM services WHERE container_name_template IS NOT NULL;  -- should be ~173
```
  </verify>
  <done>
Importer writes env_files with sort_order, container_name_template, and network names to DB. DELETE+INSERT pattern matches existing ports/volumes/env_vars. All writes in same transaction.
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` passes in api/
2. Import all 173 compose files from old-compose-and-configs/compose/
3. service_env_files has ~173 rows (one per service with env_file)
4. service_networks has ~173 rows (one per service with networks)
5. services.container_name_template populated for all 173 services
6. No seed data in migrations (PARS-07 — already verified in Phase 10)
</verification>

<success_criteria>
Parser extracts env_file, container_name, networks from compose YAML into ParsedService fields.
Importer persists all three to their respective DB tables/columns.
Full import of 173 compose files succeeds with correct row counts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-parser-importer-updates/11-01-SUMMARY.md`
</output>
