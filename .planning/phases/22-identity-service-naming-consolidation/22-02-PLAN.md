---
phase: 22-identity-service-naming-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - api/internal/orchestration/service.go
  - api/internal/compose/stack.go
  - api/internal/export/exporter.go
  - api/internal/export/importer.go
  - api/internal/lock/generator.go
  - api/internal/project/controller.go
  - api/internal/wiring/env_injector.go
  - api/internal/api/handlers/stack.go
  - api/internal/api/handlers/stack_lifecycle.go
  - api/internal/api/handlers/stack_compose.go
  - api/internal/api/handlers/stack_import.go
  - api/internal/api/handlers/instance.go
  - api/internal/api/handlers/instance_lifecycle.go
  - api/internal/api/handlers/instance_effective.go
  - api/internal/api/handlers/instance_overrides.go
  - api/internal/api/handlers/project.go
  - api/internal/api/handlers/network.go
  - api/internal/container/labels.go
  - api/internal/container/validation.go
autonomous: true

must_haves:
  truths:
    - "No fmt.Sprintf(\"devarch-...\") calls remain outside identity package"
    - "All container.ValidateName calls replaced with identity.ValidateName"
    - "All container.ContainerName calls replaced with identity.ContainerName"
    - "All container.NetworkName calls replaced with identity.NetworkName"
    - "All container.BuildLabels calls replaced with identity.BuildLabels"
    - "All container.LabelStackID references replaced with identity.LabelStackID"
    - "container/labels.go and container/validation.go contain only re-exports or are deleted"
    - "go build ./... compiles with zero errors"
  artifacts:
    - path: "api/internal/container/labels.go"
      provides: "Thin re-exports to identity package (or deleted)"
    - path: "api/internal/container/validation.go"
      provides: "Thin re-exports to identity package (or deleted)"
  key_links:
    - from: "api/internal/orchestration/service.go"
      to: "api/internal/identity"
      via: "import identity; identity.NetworkName()"
      pattern: "identity\\.NetworkName|identity\\.ContainerName"
    - from: "api/internal/api/handlers/stack.go"
      to: "api/internal/identity"
      via: "import identity; identity.ValidateName()"
      pattern: "identity\\.ValidateName|identity\\.NetworkName"
---

<objective>
Migrate all callers from container naming/validation/labels to identity package. Eliminate every ad-hoc `fmt.Sprintf("devarch-...")` call outside identity. Remove or thin-out container/labels.go and container/validation.go.

Purpose: Complete the consolidation — identity package becomes the single source of truth for all naming.
Output: Zero scattered naming calls, container package reduced to runtime concerns only.
</objective>

<execution_context>
@/home/fhcadmin/.claude/get-shit-done/workflows/execute-plan.md
@/home/fhcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-identity-service-naming-consolidation/22-RESEARCH.md
@.planning/phases/22-identity-service-naming-consolidation/22-01-SUMMARY.md
@api/internal/identity/service.go
@api/internal/identity/validation.go
@api/internal/identity/labels.go
@api/internal/container/labels.go
@api/internal/container/validation.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate internal packages to identity</name>
  <files>
    api/internal/orchestration/service.go
    api/internal/compose/stack.go
    api/internal/export/exporter.go
    api/internal/export/importer.go
    api/internal/lock/generator.go
    api/internal/project/controller.go
    api/internal/wiring/env_injector.go
  </files>
  <action>
Mechanical migration — replace container naming/validation/label calls with identity equivalents in 7 internal packages. Add `"github.com/priz/devarch-api/internal/identity"` import, remove `container` import if no longer needed (some files still need container.Client).

**orchestration/service.go:**
- `fmt.Sprintf("devarch-%s-net", stackName)` on line ~201 → `identity.NetworkName(stackName)`
- `container.ContainerName(stackName, instanceID)` on line ~94 → `identity.ContainerName(stackName, instanceID)`
- Remove `fmt` import if no longer needed (likely still needed elsewhere)

**compose/stack.go:**
- `container.ContainerName(stackName, inst.instanceID)` on line ~451 → `identity.ContainerName(stackName, inst.instanceID)`
- `container.BuildLabels(stackName, instanceID, ...)` on line ~810 → `identity.BuildLabels(stackName, instanceID, ...)`

**export/exporter.go:**
- `fmt.Sprintf("devarch-%s-net", stackName)` on line ~44 → `identity.NetworkName(stackName)`
- `container.BuildLabels(stackName, instanceID, ...)` on line ~458 → `identity.BuildLabels(stackName, instanceID, ...)`
- Remove `container` import if exporter no longer references container package

**export/importer.go:**
- `fmt.Sprintf("devarch-%s-net", file.Stack.Name)` on line ~74 → `identity.NetworkName(file.Stack.Name)`
- `fmt.Sprintf("devarch-%s-%s", file.Stack.Name, instanceName)` on line ~222 → `identity.ContainerName(file.Stack.Name, instanceName)`
- `strings.HasPrefix(key, "devarch.")` on line ~393 → `strings.HasPrefix(key, identity.LabelPrefix)`

**lock/generator.go:**
- `fmt.Sprintf("devarch-%s-net", stackName)` on line ~45 → `identity.NetworkName(stackName)`
- `extractInstanceName` function (lines 84-90) → replace body with `return identity.ExtractInstanceName(stackName, containerName)` OR delete the local function and call `identity.ExtractInstanceName` directly at the call site (line 68)
- `"devarch.stack_id"` string literal on line ~61 → `identity.LabelStackID`

**project/controller.go:**
- `fmt.Sprintf("devarch-%s-net", info.stackName)` on line ~51 → `identity.NetworkName(info.stackName)`

**wiring/env_injector.go:**
- `container.ContainerName(stackName, provider.InstanceName)` on line ~11 → `identity.ContainerName(stackName, provider.InstanceName)`
- Remove `container` import (env_injector only uses ContainerName)
  </action>
  <verify>
Run `cd /home/fhcadmin/projects/devarch/api && go build ./...` — must compile with zero errors.
Run `grep -rn 'fmt.Sprintf("devarch-' api/internal/orchestration/ api/internal/compose/ api/internal/export/ api/internal/lock/ api/internal/project/ api/internal/wiring/` — must return nothing.
  </verify>
  <done>
All 7 internal packages use identity package for naming. No ad-hoc fmt.Sprintf("devarch-...") calls remain in internal packages (excluding identity itself and handlers).
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate handlers and remove container naming/validation aliases</name>
  <files>
    api/internal/api/handlers/stack.go
    api/internal/api/handlers/stack_lifecycle.go
    api/internal/api/handlers/stack_compose.go
    api/internal/api/handlers/stack_import.go
    api/internal/api/handlers/instance.go
    api/internal/api/handlers/instance_lifecycle.go
    api/internal/api/handlers/instance_effective.go
    api/internal/api/handlers/instance_overrides.go
    api/internal/api/handlers/project.go
    api/internal/api/handlers/network.go
    api/internal/container/labels.go
    api/internal/container/validation.go
  </files>
  <action>
**Handler migration** — Replace all container naming/validation/label references with identity equivalents. Add identity import, keep container import only where container.Client is used.

**handlers/stack.go:**
- `container.NetworkName(...)` (5 calls around lines 45, 144, 545, 578, 621, 847, 959) → `identity.NetworkName(...)`
- `container.ContainerName(name, fmt.Sprintf("%d", instanceID))` on line ~1089 → `identity.ContainerName(name, fmt.Sprintf("%d", instanceID))`
- `container.ValidateName(...)` (3 calls around lines 138, 147, 842, 954) → `identity.ValidateName(...)`
- `container.LabelStackID` (2 refs around lines 36, 668) → `identity.LabelStackID`
- `fmt.Sprintf("devarch-%s-net", stackName)` on line ~62 → `identity.NetworkName(stackName)`

**handlers/stack_lifecycle.go:**
- `fmt.Sprintf("devarch-%s-net", name)` (2 calls, lines ~71 and ~157) → `identity.NetworkName(name)`

**handlers/stack_compose.go:**
- `fmt.Sprintf("devarch-%s-net", stackName)` on line ~46 → `identity.NetworkName(stackName)`

**handlers/stack_import.go:**
- `container.ValidateName(devarchFile.Stack.Name)` on line ~141 → `identity.ValidateName(devarchFile.Stack.Name)`

**handlers/instance.go:**
- `container.ValidateName(...)` (3 calls, lines ~119, ~639, ~846) → `identity.ValidateName(...)`
- `container.ValidateContainerName(...)` (3 calls, lines ~146, ~655, ~874) → `identity.ValidateContainerName(...)`
- `container.ContainerName(...)` (3 calls, lines ~151, ~667, ~879) → `identity.ContainerName(...)`

**handlers/instance_lifecycle.go:**
- `fmt.Sprintf("devarch-%s-net", stackName)` on line ~33 → `identity.NetworkName(stackName)`

**handlers/instance_effective.go:**
- `container.BuildLabels(...)` on line ~186 → `identity.BuildLabels(...)`

**handlers/instance_overrides.go:**
- `strings.HasPrefix(l.Key, "devarch.")` on line ~313 → `strings.HasPrefix(l.Key, identity.LabelPrefix)` (or use identity.ValidateLabelKey)

**handlers/project.go:**
- `container.ValidateName(req.Name)` on line ~193 → `identity.ValidateName(req.Name)`
- `container.NetworkName(req.Name)` on line ~214 → `identity.NetworkName(req.Name)`
- `container.LabelStackID` on line ~65 → `identity.LabelStackID`

**handlers/network.go:**
- `container.IsDevArchManaged(info.Labels)` on line ~83 → `identity.IsDevArchManaged(info.Labels)`
- `container.LabelManagedBy` and `container.ManagedByValue` on line ~150 → `identity.LabelManagedBy` and `identity.ManagedByValue`

**After all migrations, update container package:**

**container/labels.go** — Remove ContainerName, NetworkName, ValidateNetworkName, BuildLabels, IsDevArchManaged functions and all label constants. Keep ONLY functions that are container-runtime concerns (none remain — file can be deleted entirely if all references migrated).

**container/validation.go** — Remove ValidateName, ValidateContainerName, Slugify, dnsNamePattern, reservedNames. Delete file entirely.

Strategy: Delete container/labels.go and container/validation.go. If `go build` fails due to remaining references, those references were missed — fix them. The build is the verification.
  </action>
  <verify>
Run `cd /home/fhcadmin/projects/devarch/api && go build ./...` — must compile with zero errors.
Run `grep -rn 'fmt.Sprintf("devarch-' api/internal/ --include="*.go"` — must ONLY match files inside `api/internal/identity/`.
Run `grep -rn 'container\.ValidateName\|container\.ContainerName\|container\.NetworkName\|container\.BuildLabels\|container\.LabelStackID' api/internal/ --include="*.go"` — must return nothing.
Run `ls api/internal/container/validation.go api/internal/container/labels.go 2>&1` — files should not exist (deleted).
  </verify>
  <done>
All handlers use identity package. container/labels.go and container/validation.go deleted. No ad-hoc naming calls remain anywhere outside identity package. `go build ./...` passes.
  </done>
</task>

</tasks>

<verification>
1. `cd api && go build ./...` compiles with zero errors
2. `grep -rn 'fmt.Sprintf("devarch-' api/internal/ --include="*.go" | grep -v identity/` returns NOTHING
3. `grep -rn 'container\.ValidateName\|container\.ValidateContainerName\|container\.NetworkName\|container\.ContainerName\|container\.BuildLabels' api/internal/ --include="*.go"` returns NOTHING
4. `grep -rn 'container\.LabelStackID\|container\.LabelManagedBy\|container\.ManagedByValue\|container\.LabelPrefix' api/internal/ --include="*.go"` returns NOTHING
5. `ls api/internal/container/validation.go api/internal/container/labels.go 2>&1` — files deleted
6. `ls api/internal/container/client.go` — container package still has runtime code
7. `grep -c "identity\." api/internal/api/handlers/stack.go` — confirms identity imports in handlers
</verification>

<success_criteria>
- `go build ./...` passes with zero errors
- Zero `fmt.Sprintf("devarch-...")` calls outside identity package
- Zero `container.ValidateName`, `container.ContainerName`, `container.NetworkName`, `container.BuildLabels` calls
- Zero `container.LabelStackID` or other label constant references from container package
- container/labels.go and container/validation.go deleted
- container/client.go unchanged (runtime concerns remain)
</success_criteria>

<output>
After completion, create `.planning/phases/22-identity-service-naming-consolidation/22-02-SUMMARY.md`
</output>
