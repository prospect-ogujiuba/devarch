# 00-phase-index

Created: 2026-08-29T06:14:11.203Z
Purpose: Define the implementation order for bulk host synchronization.

# Bulk host synchronization phase index

Created: 2026-08-23
Purpose: Define the implementation order for safely synchronizing DevArch-generated `.test` domains.

## Order

1. Implement and verify the managed hosts synchronization slice in `01-managed-host-sync.md`.

## Constraints

- Preserve unrelated hosts-file content byte-for-byte where practical.
- Derive service domains from `container_name` values and app domains from routable `apps/` directories.
- Keep the existing single-host registration helper compatible.
