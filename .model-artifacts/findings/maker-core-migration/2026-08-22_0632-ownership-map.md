# ownership-map

Created: 2026-08-22T06:32:14.398Z
Purpose: Record the verified core/app ownership map before migration

# Maker core/app ownership map

## Verified repository topology

- `apps/playground/wp-content/plugins/makerblocks/` is the MakerBlocks **core repository checkout**, with remote `git@github.com:prospect-ogujiuba/makerblocks.git`. Commit `b4f8026` contains the new reusable blocks.
- `apps/playground/wp-content/themes/makerstarter/` is the MakerStarter **core repository checkout**, with remote `git@github.com:prospect-ogujiuba/makerstarter.git`. Commit `cda3893` contains the generic landing pattern.
- `apps/playground/wp-content/themes/playground-theme/` is the project-owned **app/child theme workspace**, with no remote. Commit `e54d434` contains only `templates/front-page.html`.
- `apps/playground/wp-content/plugins/playground-blocks/` is the project-owned app block workspace; no new block source was added there.

## Documented architecture

The existing phase plan explicitly states: “Continue maintaining the three nested repositories in `apps/playground/wp-content/...`.” The nesting under Playground is the integration/release checkout, not app ownership. `CORE-BOUNDARY.md` assigns reusable blocks to `plugins/makerblocks/` and project branding/templates/assets to `themes/<site>-theme/`.

## External logo change

Located uncommitted project-owned files:

- `playground-theme/assets/images/logo.png`
- `playground-theme/parts/header.html`

They were modified after the initial scaffold and belong in the Playground child theme, not MakerStarter core. The header references the logo at `/wp-content/themes/playground-theme/assets/images/logo.png`.

## Migration blocker

No separate canonical MakerBlocks/MakerStarter clone exists elsewhere under `/home/priz`; the nested checkouts are the configured core repositories. Moving files to an unspecified “core level” would contradict the current ownership plan and could duplicate or orphan the core repos. A different desired canonical path or repository layout must be named before migration.
