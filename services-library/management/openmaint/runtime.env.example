# Copy to runtime.env before starting. Never commit the populated file.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace-with-a-long-random-value
OPENMAINT_DB_PASSWORD=replace-with-another-long-random-value
JAVA_OPTS=-Xms3000m -Xmx6000m
